CLASS Test BEGIN

   VAR name = "nobody"

   INIT(who) BEGIN
      VAR name = who
   END

   FUN func() BEGIN
      RETURN "hello from " + name
   END

   FUN greet(other) BEGIN
      RETURN name + " greets " + other
   END

END
